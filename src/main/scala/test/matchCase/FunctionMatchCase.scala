package test.matchCase

import scala.collection.mutable

object FunctionMatchCase {
  def main(args:Array[String]):Unit={
    val jobMap           = new mutable.HashMap[Long, String]
    jobMap.foreach {
      case (jobId, jobTimeSpan) => {

      }
    }
  }
}
